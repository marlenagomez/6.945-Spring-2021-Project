(three)

(define (simple-add a b)
  (+ a b))

(define (lambda-add a b)
  (lambda (a b)
    (+ a b)))


(three)

(define (simple-add a b)
  (+ a b))

(define (lambda-add a b)
  (lambda (a b)
    (+ a b)))

